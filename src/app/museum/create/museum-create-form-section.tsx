import { MuseumCreateFormPresentation } from './museum-create-form-presentation'

export default async function MuseumCreateFormSection() {
  // Future: fetch default values, dropdown options, etc.
  return <MuseumCreateFormPresentation />
}
